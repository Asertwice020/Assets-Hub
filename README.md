# Assets-Hub
A centralized repository for managing and serving static assets—such as JSON configs, logos, theme files, and metadata—for multiple projects via a CDN. Each folder represents a self-contained project namespace, making it easy to organize and access project-specific static data through versioned CDN URLs.
